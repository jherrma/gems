version: '3.8'

services:
  # gem-server:
  #   # todo

  mongodb:
    image: mongo:7.0.12
    ports:
      - '27017:27017'
    volumes:
      - <replace-me>:/data/db
    networks:
      - gems-network

networks:
  gems-network:
    driver: bridge